import React, { FC } from "react";
import { HeaderLayout } from '../styles/HeaderLayout.styled';

const Header: FC = () => {

  return (
    <HeaderLayout>
      <h1>
        Cell Simulator
      </h1>
    </HeaderLayout>
  );
};

export default Header;
